


SMTP_USER=postmaster@xenoxe.com
SMTP_PASS=PostXenoxe007!
SMTP_HOST=ssl0.ovh.net
SMTP_PORT=465
# Alternative si le port 465 est bloqué: utiliser 587 avec STARTTLS
# SMTP_PORT=587

# TLS - Rejeter les certificats non autorisés (par défaut: true)
# Mettre à false uniquement si vous avez des problèmes de certificat SSL
# SMTP_TLS_REJECT_UNAUTHORIZED=false

# Configuration serveur
PORT=3000
NODE_ENV=production

# Sécurité - Clés API (séparées par des virgules pour plusieurs clés)
# Format: API_KEYS=key1,key2,key3
# Si vide, l'API est accessible sans authentification (mode développement)
API_KEYS=your-secret-api-key-here,another-key-if-needed

# CORS - Origines autorisées (séparées par des virgules)
# Format: http://domain.com,https://domain.com,http://IP:PORT
# Exemple avec IP: http://176.172.211.21 ou http://176.172.211.21:3000
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,https://xenoxe.com,http://176.172.211.21

# Autoriser les requêtes sans origine (mobile apps, Postman, etc.)
# À désactiver en production pour plus de sécurité
ALLOW_NO_ORIGIN=false
